# Sidebar configurations.
# Please be noted that a sidebar is only visible when it has at least one widget
sidebar:
  # Left sidebar configurations
  left:
    # Whether the sidebar sticks to the top when page scrolls
    sticky: true
  # Right sidebar configurations
  right:
    # Whether the sidebar sticks to the top when page scrolls
    sticky: true
# Sidebar widget configurations
# http://ppoffice.github.io/hexo-theme-icarus/categories/Widgets/
widgets:
  # Profile widget configurations
  - # Where should the widget be placed, left sidebar or right sidebar
    position: left
    type: profile
    # Author name
    author: CoolGIS
    # Author title
    author_title: Web&GIS Developer
    # Author's current location
    location: Beijing,China
    # URL or path to the avatar image
    avatar: https://cdn.jsdelivr.net/gh/CoolGIS/img-repo/img/avatar.webp
    # Whether show the rounded avatar image
    avatar_rounded: false
    # Email address for the Gravatar
    gravatar:
    # URL or path for the follow button
    follow_link: https://github.com/CoolGIS
    # Links to be shown on the bottom of the profile widget
    social_links:
      Github:
        icon: fab fa-github
        url: https://github.com/ppoffice
      掘金:
        icon: fas fa-book
        url: https://juejin.cn/user/3878732751977111
      CSDN:
        icon: fas fa-paragraph
        url: https://blog.csdn.net/weixin_47175183
      RSS:
        icon: fas fa-rss
        url: /
  # Table of contents widget configurations
  - # Where should the widget be placed, left sidebar or right sidebar
    position: left
    type: toc
    # Whether to show the index of each heading
    index: true
    # Whether to collapse sub-headings when they are out-of-view
    collapsed: true
    # Maximum level of headings to show (1-6)
    depth: 3
    # Recommendation links widget configurations
    #-
    # Where should the widget be placed, left sidebar or right sidebar
    #position: left
    #type: links
    # Names and URLs of the sites
    #links:
    #Hexo: https://hexo.io
    #Bulma: https://bulma.io
    # Categories widget configurations
    #- # Where should the widget be placed, left sidebar or right sidebar
    #  position: left
    #  type: categories
    # Recent posts widget configurations
    #- # Where should the widget be placed, left sidebar or right sidebar
    #position: left
    #type: recent_posts
  # Archives widget configurations
  #- # Where should the widget be placed, left sidebar or right sidebar
  #  position: left
  #  type: archives
  # Tags widget configurations
  - # Where should the widget be placed, left sidebar or right sidebar
    position: left
    type: tags
    # Google FeedBurner email subscription widget configurations
    #-
    # Where should the widget be placed, left sidebar or right sidebar
    #position: left
    #type: subscribe_email
    # Hint text under the email input
    #description:
    # Feedburner ID
    #feedburner_id: ''
    # Google AdSense unit configurations
    #-
    # Where should the widget be placed, left sidebar or right sidebar
    #position: left
    #type: adsense
    # AdSense client ID
    #client_id: ''
    # AdSense AD unit ID
    #slot_id: ''
    # Follow.it email subscription widget configurations
    #-
    # Where should the widget be placed, left sidebar or right sidebar
    #position: left
    #type: followit
    # Hint text under the email input
    #description:
    # Subscription form action URL
    #action_url: ''
    # Feed claiming verification code
    #verification_code: ''
